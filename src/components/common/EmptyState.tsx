export const EmptyState = ({ message }: { message: string }) => (
  <div className="app-surface app-border app-text-secondary rounded-2xl border border-dashed p-8 text-center">
    {message}
  </div>
);
